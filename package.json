{
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "8.25.0",
    "prettier": "^2.3.2",
    "prettier-eslint": "^13.0.0",
    "prettier-eslint-cli": "^5.0.1"
  },
  "scripts": {
    "format-css": "prettier --write web/src/styles/**/*.{css,scss}",
    "format-js": "prettier --write web/src/**/*.{js,jsx}",
    "format": "yarn run format-css && npm run format-js"
  }
}
